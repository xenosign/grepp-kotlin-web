<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.web.mapper.MybatisMapper">
    <select id="select" resultType="String">
        select now() as now
    </select>

    <!-- 게시글 목록 조회 -->
    <select id="boardList" resultType="com.example.web.to.BoardTO">
        SELECT
        seq,
        subject,
        writer,
        DATE_FORMAT(wdate, '%Y/%m/%d') as wdate,
        hit,
        DATEDIFF(NOW(), wdate) as wgap
        FROM board1
        ORDER BY seq DESC
    </select>

    <!-- 조회수 증가 -->
    <update id="updateHit">
        UPDATE board1
        SET hit = hit + 1
        WHERE seq = #{seq}
    </update>

    <!-- 게시글 상세 조회 -->
    <select id="boardView" resultType="com.example.web.to.BoardTO">
        SELECT
        subject,
        writer,
        mail,
        wip,
        wdate,
        hit,
        content
        FROM board1
        WHERE seq = #{seq}
    </select>

    <!-- 게시글 작성 -->
    <insert id="boardWrite">
        INSERT INTO board1
        VALUES (0, #{subject}, #{writer}, #{mail},
        SHA2(#{password}, 256), #{content}, 0, #{wip}, NOW())
    </insert>

    <!-- 게시글 수정을 위한 조회 -->
    <select id="boardModifyView" resultType="com.example.web.to.BoardTO">
        SELECT
        subject,
        writer,
        mail,
        content
        FROM board1
        WHERE seq = #{seq}
    </select>

    <!-- 게시글 수정 -->
    <update id="boardModify">
        UPDATE board1
        SET
        subject = #{subject},
        mail = #{mail},
        content = #{content}
        WHERE seq = #{seq}
        AND password = SHA2(#{password}, 256)
    </update>

    <!-- 게시글 삭제를 위한 조회 -->
    <select id="boardDeleteView" resultType="com.example.web.to.BoardTO">
        SELECT
        subject,
        writer
        FROM board1
        WHERE seq = #{seq}
    </select>

    <!-- 게시글 삭제 -->
    <delete id="boardDelete">
        DELETE FROM board1
        WHERE seq = #{seq}
        AND password = SHA2(#{password}, 256)
    </delete>
</mapper>